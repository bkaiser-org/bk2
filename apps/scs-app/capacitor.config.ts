import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.bkaiser.scs',
  appName: 'Seeclub Stäfa',
  webDir: '../../dist/apps/scs-app/browser'
};

export default config;

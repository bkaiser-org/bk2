{
  "name": "test-app-static-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve -s ./browser -l tcp://0.0.0.0:${PORT:-8080}"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
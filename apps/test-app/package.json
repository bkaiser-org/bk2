{
  "name": "test-app-firebase-build",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ../.. && pnpm install --frozen-lockfile && pnpm exec nx build test-app --configuration=production",
    "start": "cd ../.. && pnpm exec serve -s dist/apps/test-app/browser -l tcp://0.0.0.0:${PORT:-8080}"
  },
  "dependencies": {
  },
  "devDependencies": {
  },
  "engines": {
    "node": ">=23.8.0", 
    "pnpm": ">=10.11.0" 
  }
}

{
  "name": "test-app-firebase-apphosting-build",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"Triggering build from apps/test-app/package.json. CWD: $(pwd). Executing root build script...\" && npx pnpm@10.11.0 run build-test",
    "start": "echo \"Triggering start from apps/test-app/package.json. CWD: $(pwd). Executing root start script...\" && npx pnpm@10.11.0 run start-test"
  },
  "dependencies": {
  },
  "devDependencies": {
  },
  "engines": {
    "node": ">=23.8.0", 
    "pnpm": ">=10.11.0" 
  }
}

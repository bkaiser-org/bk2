/**
  * This is the server-only config.
  * It is used for server-side rendering (SSR) in Angular applications.
  */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "../../dist/out-tsc",
    "target": "es2022",
    "module": "esnext",
    "types": ["node"]
  },
  "files": [
    "src/main.server.ts"
  ],
  "include": [
    "src/**/*.d.ts"
  ]
}
/*
  This is the TypeScript configuration for the server-side rendering (SSR) build.
  It extends the main application's tsconfig.
*/
{
  "extends": "./tsconfig.app.json",
  "compilerOptions": {
    "outDir": "../../dist/out-tsc",
    "main": "src/main.server.ts",
    "types": ["node"]
  },
  "files": [
    "src/main.server.ts"
  ],
  "include": [
    "src/**/*.d.ts"
  ]
}